# 多人射击玩法开发步骤计划

## 1. 目标定位

本阶段目标是在现有多人联机第三人称项目基础上，开发一套可扩展的多人射击玩法框架。

技术方向采用业内常见的服务器权威方案：

- 第三人称肩射
- Raycast 即时命中枪
- 服务器权威命中与伤害
- 客户端本地即时反馈
- 服务器广播开火与命中表现
- 血量、死亡、延迟复活
- 为后续延迟补偿、武器配置、命中部位扩展预留结构

第一版重点不是做完整商业射击系统，而是完成一个结构合理、能联机验证、适合继续扩展的射击玩法闭环。

---

## 2. 设计原则

### 2.1 服务器权威

客户端不能自行决定命中和伤害。

客户端只负责：

- 读取输入
- 播放本地开火反馈
- 向服务器提交开火请求

服务器负责：

- 校验是否允许开火
- 执行 Raycast 命中检测
- 扣血
- 判定死亡
- 执行复活
- 广播表现事件

### 2.2 表现与逻辑分离

伤害、死亡、复活属于逻辑层。  
枪声、火光、弹道、命中特效属于表现层。

表现层可以预测或延迟播放，但不能影响最终伤害结果。

### 2.3 先闭环，后扩展

第一版只做最小射击闭环：

- 单一武器
- 玩家互相命中
- 固定伤害
- 固定复活时间

后续再扩展：

- 多武器
- 弹匣
- 换弹
- 后坐力
- 扩散
- 命中部位
- 延迟补偿

---

## 3. 模块拆分

## 3.1 `PlayerCombatController`

职责：

- 读取本地射击输入
- 控制开火冷却
- 获取相机瞄准方向
- 发送开火请求到服务器
- 播放本地即时开火反馈

建议挂载位置：

- `PlayerPrefab` 根节点

主要接口：

- `TryFire()`
- `RequestFireServerRpc(...)`
- `PlayLocalFireFeedback()`

## 3.2 `PlayerWeaponController`

职责：

- 管理当前武器
- 读取武器配置
- 计算射速冷却
- 后续扩展弹匣、换弹、切枪

建议挂载位置：

- `PlayerPrefab` 根节点

主要接口：

- `CurrentWeapon`
- `CanFire(...)`
- `ConsumeShot(...)`

## 3.3 `WeaponDefinition`

类型：

- `ScriptableObject`

职责：

- 存储武器静态配置

第一版字段：

- `weaponName`
- `damage`
- `range`
- `fireRate`
- `hitMask`
- `muzzleFlashPrefab`
- `impactPrefab`
- `fireAudioClip`
- `tracerPrefab`

后续可扩展字段：

- `magazineSize`
- `reloadTime`
- `spreadAngle`
- `recoil`
- `headshotMultiplier`

## 3.4 `PlayerHealth`

职责：

- 维护服务器权威生命值
- 同步当前血量
- 处理受伤、死亡、复活

建议挂载位置：

- `PlayerPrefab` 根节点

关键数据：

- `maxHealth`
- `CurrentHealth`
- `IsDead`
- `respawnDelay`

第一版逻辑：

- 默认血量 `100`
- 每次受击扣固定伤害
- 血量归零进入死亡
- 死亡后延迟 `3` 秒复活

## 3.5 `PlayerHitbox`

职责：

- 标记可被射击命中的玩家部位
- 将命中 collider 映射回 `PlayerHealth`

建议挂载位置：

- 玩家根节点或模型主要碰撞体

第一版：

- 只区分是否命中玩家
- 不做头部、身体倍率

后续：

- 增加命中部位枚举
- 增加伤害倍率

## 3.6 `CombatVfxController`

职责：

- 播放开火表现
- 播放命中特效
- 播放受击反馈
- 播放弹道线

设计要求：

- 不参与伤害判定
- 只消费服务器或本地预测事件

---

## 4. 射击流程

### 4.1 本地开火输入

玩家按下鼠标左键：

- 本地检查是否是 owner
- 检查玩家是否存活
- 检查当前武器是否冷却完成
- 立即播放本地枪声和枪口反馈
- 向服务器发送开火请求

### 4.2 构造瞄准射线

第三人称肩射建议使用：

- 屏幕中心相机 Ray 获取目标点
- 再从枪口或角色胸口朝目标点发射服务器 Raycast

如果第一版没有枪口点：

- 使用角色胸口或相机附近的临时 fire origin
- 后续补 `MuzzlePoint`

### 4.3 服务器校验

服务器收到开火请求后校验：

- 玩家是否存在
- 玩家是否存活
- 当前武器是否合法
- 射速是否允许
- 射击方向是否合理
- 射线起点是否离玩家过远

### 4.4 服务器命中

服务器执行 Raycast：

- 只检测玩家 hitbox layer
- 只允许命中其他玩家
- 命中后调用 `PlayerHealth.TakeDamageServer(...)`

第一版不处理：

- 穿透
- 反弹
- 爆炸范围
- 场景弹痕

### 4.5 表现广播

服务器通过 `ClientRpc` 广播：

- 开火者
- 射击起点
- 命中点或终点
- 是否命中
- 被命中玩家

所有客户端播放：

- 枪口火光
- 枪声
- 弹道线
- 命中特效
- 受击反馈

---

## 5. 死亡与复活

### 5.1 死亡

当血量小于等于 `0`：

- 服务器设置 `IsDead = true`
- 禁止该玩家移动和射击
- 广播死亡表现
- 可选隐藏或禁用碰撞体

### 5.2 复活

延迟若干秒后：

- 服务器选择复活点
- 重置血量
- 清除死亡状态
- 传送玩家到复活点
- 恢复移动和射击

第一版复活点策略：

- 先复用当前玩家生成点逻辑
- 或使用固定复活点数组

---

## 6. 开发阶段计划

## M1：基础战斗数据结构

目标：

- 搭建战斗系统脚本骨架
- 不要求完整联机表现

任务：

- 创建 `WeaponDefinition`
- 创建默认测试武器资源
- 创建 `PlayerHealth`
- 创建 `PlayerHitbox`
- 创建 `PlayerWeaponController`
- 创建 `PlayerCombatController`

验收：

- 玩家对象上能看到战斗相关组件
- 武器参数可在 Inspector 配置
- 血量可由服务器脚本修改

## M2：服务器权威 Raycast 命中

目标：

- 完成最小开火与扣血闭环

任务：

- 本地读取鼠标左键
- 客户端发送开火请求
- 服务器执行 Raycast
- 命中玩家后扣血
- 血量通过 `NetworkVariable` 同步

验收：

- 两个玩家联机
- A 开枪命中 B
- B 血量下降
- 客户端不能本地自判伤害

## M3：开火与命中表现同步

目标：

- 所有客户端都能看到开火和命中反馈

任务：

- 增加本地开火反馈
- 增加服务器广播开火表现
- 增加弹道线
- 增加命中特效
- 增加受击反馈

验收：

- 本地开火有即时反馈
- 远端能看到别人开枪
- 命中点有表现
- 表现不影响伤害判定

## M4：死亡与复活

目标：

- 完成战斗生命周期

任务：

- 血量归零进入死亡
- 死亡期间禁用移动和射击
- 延迟复活
- 复活后恢复血量和控制

验收：

- 玩家被打到 0 血后死亡
- 死亡期间不能移动和射击
- 数秒后自动复活
- 所有客户端状态一致

## M5：基础 UI

目标：

- 增加玩家可读的战斗 UI

任务：

- 屏幕中心准星
- 本地血量显示
- 可选击杀提示
- Debug 面板增加战斗信息

验收：

- 本地玩家能看到血量
- 准星位置和射击方向一致
- Debug 面板能查看血量、死亡状态、当前武器

## M6：架构增强

目标：

- 为后续正式射击玩法扩展做准备

任务：

- 将武器参数完整配置化
- 增加命中层配置
- 增加射速服务器校验
- 增加开火请求基础防作弊校验
- 预留延迟补偿输入字段

验收：

- 可以通过 `WeaponDefinition` 调整伤害、射程、射速
- 服务端拒绝非法开火
- 后续能平滑加入更多武器

## M7：延迟补偿预研

目标：

- 为射击命中公平性做技术准备

任务：

- 记录玩家最近若干 tick 的位置历史
- 开火请求携带客户端 fire sequence 或 fire time
- 服务器根据时间回溯 hitbox
- 对比无补偿和有补偿的命中体验

验收：

- 高延迟下命中体验更接近客户端视角
- 服务器仍保持最终权威

---

## 7. 第一版推荐参数

默认武器：

- 名称：`Test Rifle`
- 伤害：`25`
- 射程：`80`
- 射速：`5` 发/秒
- 弹药：第一版无限弹药

玩家生命：

- 最大血量：`100`
- 死亡复活延迟：`3` 秒

命中规则：

- 只命中玩家
- 不命中场景物体
- 不生成弹痕
- 不做命中部位倍率

---

## 8. 技术风险

### 8.1 第三人称瞄准偏差

第三人称相机和角色身体不在同一位置，可能出现：

- 准星指向目标，但枪口被墙挡住
- 角色身体朝向和相机朝向不一致
- 近距离射击偏差明显

建议处理：

- 使用相机 ray 获取目标点
- 再从枪口向目标点发射
- 增加枪口遮挡检测

### 8.2 移动同步与命中判定耦合

射击命中依赖玩家位置。  
如果移动同步不稳定，命中体验也会受影响。

建议：

- 先保持当前移动系统稳定
- 射击第一版先不追求复杂延迟补偿
- 后续再加历史快照回溯

### 8.3 客户端预测反馈与服务器结果不一致

本地可以立即播放开火反馈，但命中和伤害必须等服务器确认。

建议：

- 本地只预测枪声、火光
- 命中反馈以服务器广播为准

---

## 9. 简历可提炼技术点

完成后可提炼为：

基于 Unity NGO 实现服务器权威 TPS 射击框架，完成客户端开火请求、服务器 Raycast 命中验证、NetworkVariable 血量同步、死亡复活流程，并预留延迟补偿与武器配置扩展点。

更偏程序岗位的写法：

设计并实现多人 TPS 射击模块，采用服务器权威命中与伤害结算，拆分武器配置、玩家血量、命中体、战斗表现和复活流程，支持后续扩展延迟补偿、命中部位和多武器系统。

---

## 10. 建议下一步

下一步优先开发 `M1 + M2`：

- 先完成 `WeaponDefinition`
- 再完成 `PlayerHealth`
- 再完成 `PlayerHitbox`
- 最后接入 `PlayerCombatController` 的服务器 Raycast

只要 `M2` 跑通，就已经具备最小多人射击玩法闭环。

# 当前实现版技术设计文档

## 1. 项目概述

本项目是一个基于 Unity 的小型多人联机第三人称原型，当前已经完成以下核心能力：

- 基于 NGO（Netcode for GameObjects）的 `Host / Server / Client` 联机流程
- 自定义网络友好型角色移动器
- 第三人称本地相机跟随
- 角色基础动画、疾跑、跳跃、落地
- 运行时设置面板与调试监控面板
- 右上角圆形小地图
- 基于 URP 的角色风格化渲染尝试

当前实现目标不是完整商业化产品，而是建立一套：

- 能稳定联机
- 可继续扩展玩法
- 方便调试和多人协作

的技术底盘。

---

## 2. 技术栈

- 引擎版本：Unity 2022 LTS 系列
- 渲染管线：URP
- 联机框架：Netcode for GameObjects
- 传输层：Unity Transport
- 输入：Unity Input System
- UI：运行时动态构建 Canvas UI
- 动画：Animator Controller + 参数驱动

---

## 3. 当前整体架构

项目当前可分为五个主要层次：

### 3.1 网络会话层

负责管理联机启动模式与连接状态：

- `Host`
- `Server`
- `Client`
- `Shutdown / Disconnect`

关键脚本：

- `Assets/Scripts/Network/NetworkBootstrap.cs`
- `Assets/Scripts/Network/NetworkSessionService.cs`
- `Assets/Scripts/Network/PlayerSpawnService.cs`

职责：

- 启动 NGO 网络角色
- 配置地址与端口
- 生成和回收玩家对象
- 对 UI 层暴露当前连接状态

### 3.2 网络运动层

这是当前项目最重要的底层之一，已经不再依赖示例里的单机 `ThirdPersonController`。

关键脚本：

- `Assets/Scripts/PlayerController.cs`

职责：

- 处理输入命令采集
- 客户端预测
- 服务器权威模拟
- 状态回包与校正
- 地面检测、斜坡、台阶、碰撞收尾
- 动画参数更新

### 3.3 表现层

负责玩家模型、材质、动画、镜头和地图展示。

关键资源与脚本：

- `Assets/Prefabs/PlayerPrefab.prefab`
- `Assets/Scripts/LocalPlayerCameraController.cs`
- `Assets/UnityTechnologies/SpaceRobotKyle/...`
- `Assets/Scripts/UI/MinimapController.cs`

职责：

- 管理本地第三人称视角
- 驱动角色动画
- 小地图显示玩家与场景
- 承接角色材质与风格化渲染

### 3.4 UI 层

当前已经从 `PlayerController` 中拆分出来，形成独立 UI 体系。

关键脚本：

- `Assets/Scripts/UI/SettingsMenuController.cs`
- `Assets/Scripts/UI/DebugOverlayController.cs`
- `Assets/Scripts/UI/RuntimeCanvasUIFactory.cs`
- `Assets/Scripts/UI/RuntimeUIState.cs`

职责：

- `Esc` 设置菜单
- `F1` 调试监控面板
- 运行时 Canvas 构建
- 鼠标、菜单和角色输入阻断状态控制

### 3.5 调试数据层

关键脚本：

- `Assets/Scripts/Debug/IDebugPanelProvider.cs`
- `Assets/Scripts/Debug/DebugPanelRegistry.cs`
- `Assets/Scripts/PlayerController.cs`（实现 provider）

职责：

- 将调试信息从业务逻辑中抽离
- 允许多个系统向统一 Debug 面板供数
- 为未来扩展 `Host / Server` 专属调试数据预留接口

---

## 4. 联机模式设计

### 4.1 Host

`Host` 是“本地既是服务器又是玩家”的模式。

特点：

- 本地生成玩家
- 本地同时承担服务器权威逻辑
- 最适合单机调试或局域网主机开房

### 4.2 Server

`Server` 是“只开服务，不生成本地玩家，但保留可视化界面”的模式。

特点：

- 不创建本地玩家
- 可以作为专门的服务端窗口
- 适合开发期多人联调和状态观察

### 4.3 Client

`Client` 只负责连接已存在的 `Host` 或 `Server`。

特点：

- 生成本地玩家
- 本地具有独立相机
- 本地进行预测显示

---

## 5. 角色移动系统设计

## 5.1 设计背景

项目早期曾尝试沿用 `CharacterController + 服务器权威 + NetworkTransform` 的简单方案，但出现了：

- 客户端明显输入延迟
- 客户端位移与 Host 视角不一致
- 跳跃、斜坡、台阶、墙角处的不稳定

因此后续重构为一套更适合网络同步的自定义运动器。

## 5.2 当前方案

当前方案采用：

- 客户端输入命令采集
- 客户端预测
- 服务器权威模拟
- 状态校正与平滑吸收

核心思想：

- 不再把 Unity 示例控制器直接作为联网运动核心
- 将运动逻辑收敛到统一可控的 tick 模拟上
- 将模型表现与网络根对象分离

## 5.3 关键能力

当前运动器已支持：

- 平地移动
- 疾跑
- 跳跃
- 重力与下坠
- 斜坡
- 台阶
- 墙角碰撞稳定性增强
- 轻微地面误判缓冲
- 客户端预测与服务器校正

## 5.4 已解决的关键问题

当前版本已重点修复过：

- 非 Host 客户端明显拖延
- 位置漂移
- 斜坡穿入
- 台阶卡住
- 墙角穿墙
- 接地状态抖动
- 客户端自动重复起跳
- 跳跃落地触发过晚

## 5.5 仍可继续优化的点

- owner 端残余微抖动
- 更精细的校正平滑
- 更高阶的复杂地形边缘处理
- 更高精度的角色动画与物理表现同步

---

## 6. 动画系统设计

角色当前使用 `RobotKyle` 模型及其 Animator。

核心动画参数包括：

- `Speed`
- `MotionSpeed`
- `Grounded`
- `Jump`
- `FreeFall`

当前动画驱动原则：

- 动画参数由网络运动状态驱动
- 不再直接依赖旧的 Starter Assets 控制脚本
- 疾跑时通过倍率调整播放节奏
- 起跳、落地、空中状态由移动器统一切换

已做过的重要调整：

- 禁用旧的 `ThirdPersonController` 本地移动控制
- 修复动画事件空引用问题
- 调整落地过渡时间，减少“落地晚半拍”

---

## 7. 相机系统设计

本地第三人称相机为“仅本地拥有者创建”的结构。

关键脚本：

- `Assets/Scripts/LocalPlayerCameraController.cs`

设计原则：

- 每个玩家只拥有自己的本地相机
- 相机不参与网络同步
- 鼠标输入仅作用于本地视角
- 设置菜单打开时停止角色镜头输入

优势：

- 避免多玩家共享相机
- 联机结构更清晰
- 后续易扩展缩放、锁定目标、肩部视角切换等功能

---

## 8. UI 系统设计

## 8.1 设计目标

UI 系统当前目标不是完整美术成品，而是：

- 不依赖手工场景搭建即可运行
- 结构清楚
- 便于快速迭代
- 与联机状态和角色输入联动

## 8.2 设置面板

`Esc` 调出的设置面板分为五页：

- 游戏玩法说明
- 创建房主主机
- 创建服务器
- 创建客户端
- 当前连接状态

设计特性：

- 菜单打开时解锁鼠标
- 菜单打开时阻断角色输入与镜头输入
- 不暂停游戏时间
- 兼顾开发调试与实际可用性

## 8.3 Debug 面板

`F1` 打开左侧半透明调试面板。

设计特性：

- 不抢夺输入焦点
- 不阻断角色操作
- 通过 provider 注册调试数据
- 为未来扩展更多调试分组预留空间

当前可监控的重点数据包括：

- 网络延迟相关指标
- 输入队列
- 预测与校正误差
- 玩家移动和接地状态

---

## 9. 小地图系统设计

## 9.1 方案演进

小地图最初尝试过“建筑轮廓代理渲染”的风格化方案，但由于：

- 大量模型不可读
- 建筑代理筛选和轮廓计算复杂
- 美术维护成本高

最终当前版本改回更稳的方案：

- 俯视正交相机直拍真实场景
- 深蓝色压暗覆盖
- 右上角圆形显示
- 黑色外边框
- 玩家箭头叠加显示

## 9.2 当前设计特点

- 地图北朝上
- 本地玩家始终位于中心
- 自己用高亮箭头
- 其他玩家用另一种颜色箭头
- 小地图在设置面板打开时隐藏

## 9.3 技术取舍

当前方案的优点：

- 稳定
- 不依赖建筑代理
- 不需要手工标记建筑
- 不涉及 mesh 顶点可读性问题

代价：

- 风格化程度不如完全定制地图
- 建筑显示更接近真实场景俯拍

这是当前阶段更合适的工程化选择。

---

## 10. 渲染与风格化方向

项目当前基于 URP。

已做过的相关尝试包括：

- 修复导入资源后地面粉色材质问题
- 统一检查 URP 材质引用
- 接入角色 toon shader 试验
- 调整阴影、雾、环境色和反射影响

当前美术方向更偏：

- 角色风格化
- 场景保留一定真实结构
- 整体比默认 Starter Assets 更干净

当前经验结论：

- 角色 toon 化收益高
- 场景没必要过早做全量 toon shader
- 先保证联机、移动和玩法原型稳定更重要

---

## 11. Prefab 与场景职责

### 11.1 `PlayerPrefab`

当前承载：

- `NetworkObject`
- 自定义 `PlayerController`
- `CharacterController`（提供角色尺寸与参数参考）
- 本地相机控制
- 小地图控制

### 11.2 场景

当前主场景已经切到 `Playground`。

场景中承载：

- 地形
- 建筑
- 灯光
- 反射探针
- 联机测试区域

场景资源已经做过一次较保守的瘦身与整理，以兼顾：

- 可推送到 GitHub
- 美术资源完整性
- 联机原型可运行

---

## 12. 资源管理与仓库策略

项目中曾遇到的一个实际问题是：导入资源包后，仓库内大模型、贴图和素材源文件体量过大。

当前经验与策略：

- 运行所需资源保留在主仓库
- 不必要的源文件、重复格式和素材备份应尽量移出主仓库
- 避免把整个素材包原始资产无差别提交
- 优先保证 `Assets / Packages / ProjectSettings` 的开发完整性

对于 Unity 协作项目，这一步非常关键，因为它直接影响：

- 克隆速度
- 拉取速度
- 仓库稳定性
- 团队协作成本

---

## 13. 调试与联调经验

当前项目已形成一些稳定的联调方法：

- Editor 开 `Host`
- 打包客户端加入
- 打包 Host + Editor Client
- 局域网多机互联

调试经验：

- `Host` 模式适合快速改代码验证
- `Server + 多 Client` 更适合查纯同步问题
- `F1` Debug 面板适合持续监控预测与校正数据
- 小地图和设置菜单已足够支持多人基本测试流程

---

## 14. 当前系统的优点

- 联机移动底盘已经基本成型
- `Host / Server / Client` 模式清晰
- UI 已从核心控制逻辑中拆分
- 小地图和调试工具已经实用
- 角色、相机、动画与网络之间职责边界较明确

---

## 15. 后续推荐开发方向

建议后续按这个优先级推进：

### 15.1 进一步优化联机手感

- 继续压低客户端残余微抖
- 优化校正误差吸收
- 让 owner 端视觉更平顺

### 15.2 扩展玩法层

- 交互
- 攻击
- 受击
- 技能
- 物品

### 15.3 增加多人状态 UI

- 玩家列表
- 房间信息
- Ping/状态简报
- 服务端状态面板

### 15.4 美术与体验打磨

- 更统一的场景风格
- 更成熟的小地图样式
- 正式菜单样式和动效

---

## 16. 总结

当前项目已经从“Starter Assets + NGO 原型”发展为一套具备独立底盘的多人联机原型。

最有价值的几个技术成果是：

- 将玩家移动重构为更适合网络同步的自定义运动器
- 将 UI 和调试能力从核心角色逻辑中拆离
- 建立了可继续扩展的联机、相机、动画、小地图和调试框架

这意味着项目后续无论是继续做玩法、做更多美术风格化，还是扩展为更完整的多人联机 Demo，都已经有了一个比较稳的技术基础。
